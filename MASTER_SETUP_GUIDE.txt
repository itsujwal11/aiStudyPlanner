================================================================================
                    AI STUDY PLANNER - MASTER SETUP GUIDE
                         Complete Setup from Beginning to End
================================================================================

TEST USER CREDENTIALS
================================================================================
Email:    test@example.com
Password: Test@123456

Database User: aasa_user
Database Password: aasa_password

================================================================================
PART 1: ORACLE DATABASE SETUP (SQL*PLUS)
================================================================================

STEP 1: Open SQL*Plus as Administrator
--------
Press Win + R
Type: sqlplus / as sysdba
Press Enter

STEP 2: Create Oracle User
--------
In SQL*Plus, execute these commands one by one:

CREATE USER aasa_user IDENTIFIED BY aasa_password;

GRANT CONNECT, RESOURCE TO aasa_user;

GRANT CREATE SESSION TO aasa_user;

GRANT UNLIMITED TABLESPACE TO aasa_user;

Expected Output: User created / Grant succeeded

STEP 3: Exit SQL*Plus
--------
EXIT;

STEP 4: Connect as New User
--------
sqlplus aasa_user/aasa_password

STEP 5: Create All Database Tables
--------
Copy and paste ALL of the following SQL commands into SQL*Plus:

================================================================================
COMPLETE DATABASE SCHEMA - COPY ALL OF THIS
================================================================================

-- Users table
CREATE TABLE users (
    id NUMBER PRIMARY KEY,
    email VARCHAR2(255) NOT NULL UNIQUE,
    name VARCHAR2(255) NOT NULL,
    password VARCHAR2(255) NOT NULL,
    created_at TIMESTAMP DEFAULT SYSDATE NOT NULL,
    updated_at TIMESTAMP DEFAULT SYSDATE NOT NULL
);

-- Create sequence for users
CREATE SEQUENCE users_seq START WITH 1 INCREMENT BY 1;

-- PDF Documents table
CREATE TABLE pdf_documents (
    id NUMBER PRIMARY KEY,
    user_id NUMBER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    file_name VARCHAR2(255) NOT NULL,
    file_path VARCHAR2(500) NOT NULL,
    upload_date TIMESTAMP DEFAULT SYSDATE NOT NULL,
    exam_date DATE NOT NULL,
    extracted_text CLOB,
    is_analyzed NUMBER(1) DEFAULT 0
);

CREATE SEQUENCE pdf_documents_seq START WITH 1 INCREMENT BY 1;

-- Topics table
CREATE TABLE topics (
    id NUMBER PRIMARY KEY,
    pdf_id NUMBER NOT NULL REFERENCES pdf_documents(id) ON DELETE CASCADE,
    title VARCHAR2(255) NOT NULL,
    description CLOB,
    concept_density NUMBER(10,2),
    keyword_difficulty NUMBER(10,2),
    formula_count NUMBER,
    content_length NUMBER,
    complexity_score NUMBER(10,2),
    importance_score NUMBER(10,2),
    priority_score NUMBER(10,2)
);

CREATE SEQUENCE topics_seq START WITH 1 INCREMENT BY 1;

-- Quizzes table
CREATE TABLE quizzes (
    id NUMBER PRIMARY KEY,
    topic_id NUMBER NOT NULL REFERENCES topics(id) ON DELETE CASCADE,
    question CLOB NOT NULL,
    option_a VARCHAR2(500) NOT NULL,
    option_b VARCHAR2(500) NOT NULL,
    option_c VARCHAR2(500) NOT NULL,
    option_d VARCHAR2(500) NOT NULL,
    correct_answer VARCHAR2(500) NOT NULL,
    difficulty VARCHAR2(50) NOT NULL,
    explanation CLOB
);

CREATE SEQUENCE quizzes_seq START WITH 1 INCREMENT BY 1;

-- Quiz Attempts table
CREATE TABLE quiz_attempts (
    id NUMBER PRIMARY KEY,
    user_id NUMBER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    quiz_id NUMBER NOT NULL REFERENCES quizzes(id) ON DELETE CASCADE,
    selected_answer VARCHAR2(500) NOT NULL,
    is_correct NUMBER(1) NOT NULL,
    marks_obtained NUMBER(10,2),
    attempt_time TIMESTAMP DEFAULT SYSDATE NOT NULL,
    time_taken_seconds NUMBER
);

CREATE SEQUENCE quiz_attempts_seq START WITH 1 INCREMENT BY 1;

-- Study Progress table
CREATE TABLE study_progress (
    id NUMBER PRIMARY KEY,
    user_id NUMBER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    topic_id NUMBER NOT NULL REFERENCES topics(id) ON DELETE CASCADE,
    weakness_level VARCHAR2(50),
    completion_percentage NUMBER(10,2) DEFAULT 0,
    best_score NUMBER(10,2) DEFAULT 0,
    total_attempts NUMBER DEFAULT 0,
    correct_attempts NUMBER DEFAULT 0,
    UNIQUE(user_id, topic_id)
);

CREATE SEQUENCE study_progress_seq START WITH 1 INCREMENT BY 1;

-- Create indexes for performance
CREATE INDEX idx_pdf_user ON pdf_documents(user_id);
CREATE INDEX idx_topic_pdf ON topics(pdf_id);
CREATE INDEX idx_quiz_topic ON quizzes(topic_id);
CREATE INDEX idx_attempt_user ON quiz_attempts(user_id);
CREATE INDEX idx_attempt_quiz ON quiz_attempts(quiz_id);
CREATE INDEX idx_progress_user ON study_progress(user_id);
CREATE INDEX idx_progress_topic ON study_progress(topic_id);
CREATE INDEX idx_user_email ON users(email);

COMMIT;

================================================================================
END OF DATABASE SCHEMA
================================================================================

STEP 6: Verify Tables Were Created
--------
In SQL*Plus, execute:

DESC users;

You should see:
Name                                      Null?    Type
----------------------------------------- -------- ----------------------------
ID                                                 NUMBER
EMAIL                                    NOT NULL VARCHAR2(255)
NAME                                     NOT NULL VARCHAR2(255)
PASSWORD                                 NOT NULL VARCHAR2(255)
CREATED_AT                               NOT NULL TIMESTAMP(6)
UPDATED_AT                               NOT NULL TIMESTAMP(6)

STEP 7: Verify All Tables Exist
--------
SELECT TABLE_NAME FROM USER_TABLES;

You should see:
TABLE_NAME
------------------------------
USERS
PDF_DOCUMENTS
TOPICS
QUIZZES
QUIZ_ATTEMPTS
STUDY_PROGRESS

STEP 8: Exit SQL*Plus
--------
EXIT;

================================================================================
PART 2: BACKEND SETUP (SPRING BOOT)
================================================================================

STEP 1: Navigate to Backend Directory
--------
Open PowerShell and run:

cd C:\Users\Ujwal\Desktop\aiStudyPlanner\backend

STEP 2: Verify .env File Exists
--------
The .env file should already exist with Oracle configuration.
Check it contains:

SPRING_DATASOURCE_URL=jdbc:oracle:thin:@localhost:1521:orcl
SPRING_DATASOURCE_USERNAME=aasa_user
SPRING_DATASOURCE_PASSWORD=aasa_password
SPRING_DATASOURCE_DRIVER_CLASS_NAME=oracle.jdbc.driver.OracleDriver

STEP 3: Build Backend
--------
Run:

mvn clean install

Expected Output:
[INFO] Building jar: C:\...\backend\target\aasa-backend-1.0.0.jar
[INFO] BUILD SUCCESS

STEP 4: Run Backend
--------
Run:

mvn spring-boot:run

Expected Output:
Started AasaBackendApplication in X.XXX seconds
Tomcat started on port(s): 8080 (http)

IMPORTANT: Keep this terminal open. Backend must stay running.

================================================================================
PART 3: FRONTEND SETUP (REACT)
================================================================================

STEP 1: Open New PowerShell Terminal
--------
DO NOT close the backend terminal. Open a NEW PowerShell window.

STEP 2: Navigate to Frontend Directory
--------
cd C:\Users\Ujwal\Desktop\aiStudyPlanner\frontend

STEP 3: Install Dependencies
--------
Run:

npm install

Expected Output:
added XXX packages in X.XXs

STEP 4: Start Frontend
--------
Run:

npm run dev

Expected Output:
VITE v5.0.8 ready in XXX ms

➜  Local:   http://localhost:5173/
➜  press h to show help

IMPORTANT: Keep this terminal open. Frontend must stay running.

================================================================================
PART 4: REGISTER AND LOGIN TEST USER
================================================================================

STEP 1: Open Browser
--------
Open your web browser and go to:

http://localhost:5173

STEP 2: Click Register Button
--------
You should see a login page. Look for "Register" link or button.
Click it.

STEP 3: Fill Registration Form
--------
Enter the following information:

Email:    test@example.com
Name:     Test User
Password: Test@123456

STEP 4: Click Register Button
--------
Click the "Register" button.

Expected: You will be redirected to the login page.

STEP 5: Login with Test Credentials
--------
Enter:

Email:    test@example.com
Password: Test@123456

Click "Login" button.

Expected: You will see the dashboard with:
- User name displayed
- Navigation menu
- Dashboard statistics
- Upload PDF section

================================================================================
PART 5: TEST THE APPLICATION
================================================================================

STEP 1: Verify You Are Logged In
--------
You should see:
- Your name (Test User) displayed
- Dashboard with statistics
- Menu options (Upload PDF, Quizzes, etc.)

STEP 2: Test PDF Upload (Optional)
--------
1. Click "Upload PDF" button
2. Select any PDF file from your computer
3. Set exam date
4. Click "Upload"

Expected: PDF appears in your list

STEP 3: Verify Database (Optional)
--------
Open a NEW PowerShell window and run:

sqlplus aasa_user/aasa_password

Then execute:

SELECT COUNT(*) FROM users;

Expected Output:
  COUNT(*)
----------
         1

This confirms your user was created in the database.

STEP 4: Check Other Tables (Optional)
--------
SELECT COUNT(*) FROM pdf_documents;
SELECT COUNT(*) FROM topics;
SELECT COUNT(*) FROM quizzes;
SELECT COUNT(*) FROM quiz_attempts;
SELECT COUNT(*) FROM study_progress;

Type EXIT; to exit SQL*Plus

================================================================================
PART 6: DOCKER SETUP (OPTIONAL)
================================================================================

If you want to run everything in Docker containers:

STEP 1: Stop Current Services
--------
In backend terminal: Press Ctrl + C
In frontend terminal: Press Ctrl + C

STEP 2: Build Docker Images
--------
cd C:\Users\Ujwal\Desktop\aiStudyPlanner

docker-compose build

Expected Output:
[+] Building 45.6s
✔ postgres Pulled
✔ backend built
✔ frontend built

STEP 3: Start Services
--------
docker-compose up -d

Expected Output:
[+] Running 3/3
 ✔ Container aasa-postgres   Started
 ✔ Container aasa-backend    Started
 ✔ Container aasa-frontend   Started

STEP 4: Access Services
--------
Frontend:  http://localhost:3000
Backend:   http://localhost:8080

Login with same credentials:
Email:    test@example.com
Password: Test@123456

STEP 5: Stop Services
--------
docker-compose down

================================================================================
TROUBLESHOOTING
================================================================================

PROBLEM: "ORA-12514: TNS:listener does not currently know of service requested"
SOLUTION:
- Check if Oracle listener is running
- Open PowerShell and run: lsnrctl status
- If not running, start it: lsnrctl start

PROBLEM: "Address already in use" (port 8080 or 5173)
SOLUTION:
- Find process using port: netstat -ano | findstr :8080
- Kill process: taskkill /PID <PID> /F
- Try again

PROBLEM: "BUILD FAILURE" in Maven
SOLUTION:
- Clear cache: mvn clean
- Update dependencies: mvn dependency:resolve
- Try again: mvn clean install

PROBLEM: Frontend shows blank page or CORS error
SOLUTION:
- Clear node modules: rm -r node_modules package-lock.json
- Reinstall: npm install
- Clear Vite cache: rm -r .vite
- Restart: npm run dev

PROBLEM: Cannot connect to database
SOLUTION:
- Verify Oracle is running: sqlplus / as sysdba
- Check user exists: SELECT USERNAME FROM DBA_USERS WHERE USERNAME='AASA_USER';
- Verify connection string in backend/.env

================================================================================
QUICK REFERENCE
================================================================================

SERVICE URLS:
Frontend:  http://localhost:5173
Backend:   http://localhost:8080
Database:  localhost:1521

TEST CREDENTIALS:
Email:    test@example.com
Password: Test@123456

DATABASE CREDENTIALS:
Username: aasa_user
Password: aasa_password

KEY COMMANDS:
# Database
sqlplus / as sysdba                    (Connect as admin)
sqlplus aasa_user/aasa_password        (Connect as user)
DESC users;                            (Check table structure)
SELECT COUNT(*) FROM users;            (Count records)

# Backend
mvn clean install                      (Build)
mvn spring-boot:run                    (Run)

# Frontend
npm install                            (Install dependencies)
npm run dev                            (Run development server)

# Docker
docker-compose build                   (Build images)
docker-compose up -d                   (Start services)
docker-compose logs -f                 (View logs)
docker-compose down                    (Stop services)

================================================================================
DATABASE TABLES SUMMARY
================================================================================

1. USERS
   - Stores user account information
   - Fields: id, email, name, password, created_at, updated_at
   - Primary Key: id
   - Unique: email

2. PDF_DOCUMENTS
   - Stores uploaded PDF files
   - Fields: id, user_id, file_name, file_path, upload_date, exam_date, extracted_text, is_analyzed
   - Foreign Key: user_id -> users(id)

3. TOPICS
   - Stores topics extracted from PDFs
   - Fields: id, pdf_id, title, description, complexity_score, importance_score, priority_score, etc.
   - Foreign Key: pdf_id -> pdf_documents(id)

4. QUIZZES
   - Stores quiz questions
   - Fields: id, topic_id, question, option_a, option_b, option_c, option_d, correct_answer, difficulty, explanation
   - Foreign Key: topic_id -> topics(id)

5. QUIZ_ATTEMPTS
   - Stores user quiz attempts and answers
   - Fields: id, user_id, quiz_id, selected_answer, is_correct, marks_obtained, attempt_time, time_taken_seconds
   - Foreign Keys: user_id -> users(id), quiz_id -> quizzes(id)

6. STUDY_PROGRESS
   - Tracks user progress on topics
   - Fields: id, user_id, topic_id, weakness_level, completion_percentage, best_score, total_attempts, correct_attempts
   - Foreign Keys: user_id -> users(id), topic_id -> topics(id)

================================================================================
VERIFICATION CHECKLIST
================================================================================

Database Setup:
[ ] Oracle user aasa_user created
[ ] All 6 tables created
[ ] All sequences created
[ ] All indexes created
[ ] Can connect as aasa_user

Backend Setup:
[ ] .env file exists with Oracle config
[ ] mvn clean install succeeds
[ ] Backend runs on port 8080
[ ] No connection errors in logs

Frontend Setup:
[ ] .env file exists with API URL
[ ] npm install succeeds
[ ] Frontend runs on port 5173
[ ] No CORS errors in browser console

Testing:
[ ] Can register new user
[ ] Can login with test@example.com / Test@123456
[ ] Dashboard displays correctly
[ ] User appears in database (SELECT COUNT(*) FROM users;)

================================================================================
NEXT STEPS
================================================================================

1. Follow PART 1 to create Oracle database and tables
2. Follow PART 2 to build and run backend
3. Follow PART 3 to install and run frontend
4. Follow PART 4 to register and login test user
5. Follow PART 5 to test the application
6. (Optional) Follow PART 6 to setup Docker

Once everything is working:
- Upload a PDF to test the system
- Explore the dashboard
- Take a quiz to test the quiz system
- Check database records to verify data is saved

================================================================================
SUPPORT
================================================================================

If you encounter any issues:

1. Check the TROUBLESHOOTING section above
2. Verify all prerequisites are installed
3. Check that all configuration files are correct
4. Review the error messages in the console
5. Verify database tables exist with DESC command
6. Check that ports 8080, 5173, and 1521 are available

================================================================================
END OF MASTER SETUP GUIDE
================================================================================

Created: May 23, 2026
Project: AI Study Planner
Version: 1.0.0
Status: Ready to Deploy

For any questions, refer to the relevant section above.
